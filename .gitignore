__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# data and run artifacts
data/
runs/
checkpoints/
results/
# ...but keep the written-up results, which are the point of the project
!results/
results/*.pt
results/runs/
*.pth
*.pt
*.npy
*.xyz
*.off
# benchmark archives (224 MB, see docs/benchmark.md to re-download)
data/*.zip

# Kaggle upload bundles (regenerate with scripts/package_for_kaggle.py)
kaggle/*.zip

# stray checkpoint archive, never belonged in the repo
best.zip
